
#define  MAX 50 //кол-во интервалов макс  –≈јЋ№Ќќ на 2 меньше!!!

void du_init();
uint du(void);
void read(void);
char du_main(uchar pr);
bool rw(bool a,char b,uint c); 
void compr (void);
void coding(uint i, uchar pr) ;

  //extern uchar  perepoln;//кол-во переполнений счетчика
  //extern uchar t1,t2;
  static bool ini; 
  uint dim1[MAX],dim2[MAX];
  extern char ct;
  uint minimallen;    
  float len[MAX];
  static bool intok,readint,big;
  static uint last;
  extern char cod_ok;
  uchar c1,c2,c3;
  char ind;


   void du_init(void);
  char du_main(uchar pr) ;
 
  



